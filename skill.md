# สิ่งที่ผม (Claude) ยังขาด — สำหรับโปรเจกต์ A GLEAM

ไฟล์นี้เป็นบันทึกความจริงใจว่าตอนนี้ผมทำอะไรได้/ไม่ได้ดีพอสำหรับเป้าหมาย "เว็บสมบูรณ์ ไร้ช่องโหว่"
ของคุณ และเป็นที่เก็บสิ่งที่ไปค้นคว้าเพิ่มมาระหว่างทาง จะอัปเดตทุกครั้งที่ปิดช่องว่างได้จริง
(ไม่ใช่แค่คิดว่าปิดได้)

ข้อเท็จจริงที่ต้องพูดตรงๆ ก่อน: **"ไร้ช่องโหว่ 100%" ไม่มีจริงสำหรับซอฟต์แวร์ใดๆ** สิ่งที่ทำได้คือ
ลดความเสี่ยงเป็นชั้นๆ (defense in depth) + มีทางรู้เร็วเมื่อมีปัญหา + มีแผนรับมือ ไม่ใช่ทำครั้งเดียวจบ

---

## ช่องว่างที่ผมแก้เองไม่ได้ (ต้องใช้คนจริง/บริการภายนอก)

1. **กฎหมาย/PDPA/สคบ. แบบรับรองได้จริง** — ผมช่วยร่างและเช็คเบื้องต้นได้ (เช่นหน้า
   `/privacy-policy`, `/terms` ที่มีอยู่แล้ว) แต่ผมไม่ใช่ทนาย ไม่สามารถรับรองว่าถูกต้องตามกฎหมายไทย
   100% ได้ — เรื่องสำคัญ (การเก็บข้อมูลบัตร/สลิป, สิทธิ์ผู้บริโภคออนไลน์, ใบกำกับภาษี) ควรให้ทนาย/
   นักบัญชีที่มีใบอนุญาตช่วยตรวจอีกชั้นก่อนเปิดขายจริงจัง
2. **Penetration testing จริงจากภายนอก** — ที่ผมทำมาตลอด (audit หลายรอบ) คือ code review ตาม
   checklist (OWASP-style) ซึ่งจับ pattern ที่รู้จักได้ดี แต่ต่างจาก pentest จริงที่มี fuzzing แบบสุ่ม,
   ยิง payload แปลกๆ จาก IP ภายนอกจริง, และทดสอบ timing/race condition แบบ automated
   → ถ้าอยากมั่นใจสูงสุดก่อนเปิดขายใหญ่ ควรจ้างบริการ pentest จริงสักรอบ
3. **Load testing ระดับ production** — ไม่เคยทดสอบว่าเว็บรับมือยังไงถ้ามีคนสั่งซื้อพร้อมกันเยอะๆ
   (เช่น ตอนไลฟ์ขายของ/ลดราคาใหญ่) ต้องใช้เครื่องมือยิง traffic จำลอง (k6, Artillery) ซึ่งควรทำแบบ
   ระวังไม่ให้กระทบผู้ใช้จริงหรือ rate limit ของ Supabase/Vercel

## ช่องว่างที่แก้ได้ (ของจริง ไม่ใช่ทฤษฎี) — ยังไม่ได้ปิด

- [ ] **ไม่มี error monitoring ใน production** — ตอนนี้ผมรู้ว่ามี bug ก็ต่อเมื่อคุณมาบอก หรือผมมานั่งเทส
      เองเท่านั้น ไม่มีระบบแจ้งเตือนอัตโนมัติเมื่อโค้ดพังจริงบนเว็บที่ลูกค้าใช้อยู่ — ควรมี error tracking
      (ดูผลค้นคว้าด้านล่าง)
- [ ] **ไม่มี dependency vulnerability monitoring ต่อเนื่อง** — `npm audit fix` เป็นการเช็คครั้งเดียว
      ถ้ามี CVE ใหม่ประกาศทีหลังในไลบรารีที่ใช้อยู่ ไม่มีอะไรมาเตือน (GitHub Dependabot alerts
      เปิดได้ฟรีถ้า repo อยู่บน GitHub — ยังไม่ได้เปิด)
- [ ] **ไม่เคยทดสอบ restore จาก Supabase backup จริง** — "มี backup" ไม่มีความหมายถ้าไม่เคยลองกู้
      คืนจริงสักครั้งว่าทำได้และข้อมูลครบ
- [ ] **ไม่เคยทำ accessibility (a11y) audit** — ยังไม่เช็คว่าใช้ screen reader ได้ไหม, contrast สีพอไหม
      ตาม WCAG, กด tab ไล่ทุกปุ่มได้ครบไหม (เคยแก้แค่ bug tab-order เข้าปุ่ม show/hide password
      ที่คุณเจอเอง ไม่ใช่ audit เต็มรูปแบบ)
- [ ] **Slip2Go: ยังไม่ยืนยัน response contract ของ checkDuplicate/checkReceiver** — ส่งเงื่อนไขไปใน
      request แล้ว แต่ไม่รู้แน่ชัดว่า response จะบอกผลลัพธ์ตรงไหน/โค้ดอะไร (ตอนนี้ fallback เป็น
      "error"/ให้แอดมินดูเองอย่างปลอดภัยไว้ก่อน) — บล็อกอยู่ที่เอกสาร Slip2Go หรือบัญชีจริงที่ยังไม่หมด
      อายุทดลอง
- [ ] **ไม่มีระบบแจ้งเตือนลูกค้า (อีเมล/SMS) เมื่อสถานะออเดอร์เปลี่ยน** — ตอนนี้ลูกค้าต้องเข้ามาเช็ค
      เอง ยังไม่มี integration ส่งแจ้งเตือนอัตโนมัติ

## สิ่งที่ไปค้นคว้ามาแล้ว (log การเรียนรู้จริง)

### 2569-08-03 — Error monitoring สำหรับ Next.js App Router
ค้นดูวิธีติดตั้ง Sentry บน Next.js App Router (ทางเลือกที่ใช้กันมากสุดสำหรับ error tracking ฟรี):
- Free tier ของ Sentry ให้ 5,000 errors/เดือน — พอสำหรับร้านขนาดนี้
- วิธีติดตั้งมาตรฐานคือรัน wizard: `npx @sentry/wizard@latest -i nextjs` จะสร้างไฟล์
  `instrumentation.ts` ที่ root ของโปรเจกต์ (ไม่ใช่ใน `app/`) แยก config สำหรับ Node.js runtime
  (server components/API routes) กับ edge runtime (middleware) เพราะเป็นคนละ environment
- **สิ่งที่ยังไม่ยืนยัน (ต้องเช็คก่อนใช้จริง):** โปรเจกต์นี้ใช้ Next.js เวอร์ชันที่ต่างจากที่ทั่วไปรู้จัก
  (`middleware.ts` เปลี่ยนชื่อเป็น `proxy.ts` แล้วตามที่บันทึกไว้ใน AGENTS.md) — wizard ของ Sentry
  รุ่นล่าสุดอาจยังอ้างอิงชื่อไฟล์/convention แบบเก่า ต้องอ่าน `node_modules/next/dist/docs/` เทียบกับ
  เอกสาร Sentry ก่อนรัน wizard จริง ไม่ควรเชื่อ wizard เขียนถูกอัตโนมัติ 100%
- Sources:
  - [Automatic Instrumentation | Sentry for Next.js](https://docs.sentry.io/platforms/javascript/guides/nextjs/tracing/instrumentation/automatic-instrumentation/)
  - [Sentry + Next.js: Complete Error Monitoring Guide (2026)](https://stacknotice.com/blog/sentry-nextjs-complete-guide-2026)

---

## วิธีใช้ไฟล์นี้
- ก่อนบอกว่า "เว็บนี้ปลอดภัย/สมบูรณ์แล้ว" ให้เช็คไฟล์นี้ก่อนว่ายังมีอะไรค้างอยู่ในหมวด "ยังไม่ได้ปิด"
- เมื่อปิดช่องว่างไหนได้จริง (ทำงานได้จริง ทดสอบแล้ว ไม่ใช่แค่เขียนโค้ด) ให้ย้ายจาก "ยังไม่ได้ปิด"
  มาเป็นบันทึกด้านล่างพร้อมวันที่และรายละเอียดว่าทำอะไรไป
- เพิ่มหัวข้อใหม่ได้เรื่อยๆ เมื่อเจอช่องว่างใหม่ระหว่างทำงาน (เช่นตอน audit รอบต่อไป)
